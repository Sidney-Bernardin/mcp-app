[project]
name = "rollbringer-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sidney Bernardin", email = "sidney.bernardin@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "mcp[cli]>=2.1.1",
    "pydantic>=2.13.5",
]

# [project.scripts]
# main = "mcp_test:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
